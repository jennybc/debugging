template <- "${EXCLAMATION} - your reprex is ${adjective}!"
praise(template)

#+ include = FALSE
rm(template)

#+ include = TRUE
library(praise)
praise(template)

library(praise)
template <- "${EXCLAMATION} - your reprex is ${adjective}!"
praise(template)
